# ZeroPath Benchmarks

These benchmark skeletons define expected evidence-first behavior. They are not
full vulnerable protocol fixtures yet.

Each `expected.json` states the ecosystem, adapter, protocol type, expected
invariant, expected sequence shape, and whether the engine should find or reject
the scenario once full fixtures are added.
